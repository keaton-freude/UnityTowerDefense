using UnityEngine;
using System.Collections;

public class LoadParameters : MonoBehaviour {
	
	public bool CreateServer; //true if the user wants to create a server
	public string IPAddress;  //The IP Address to use if CreateServer is false (wants to JOIN a server)
	
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
